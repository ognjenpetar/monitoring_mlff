__pycache__/
*.pyc
.env
.venv/
.worktrees/

# Local SQLite stats DB (from running service.py outside Docker)
*.db
data/

# SSH private keys (Oracle Cloud etc.) — never commit
*.key
*.pem

# Contains live SMTP/Telegram credentials
config.json
stabilna verzija/config.json

# Claude Code local settings
.claude/settings.local.json
